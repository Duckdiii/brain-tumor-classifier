# Hyperparameters for the custom GeneralCNN classifier.
model: general_cnn
num_classes: 4
image_size: 640
batch_size: 32
epochs: 100
optimizer: adamw
lr: 0.0001
weight_decay: 0.0001
evaluation_metrics: [accuracy, loss]
